{
  "name": "atas-journal-browser-harness",
  "private": true,
  "type": "module",
  "description": "Dev-only browser harness. Deliberately its own package: Playwright is a tool for looking at the app, not a dependency of it.",
  "devDependencies": {
    "playwright": "^1.49.0"
  }
}
