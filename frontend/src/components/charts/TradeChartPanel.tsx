import type { FilterScope } from "../../lib/queryKeys";

// Placeholder until Phase 4 (candlestick reconstruction + excursion).
export function TradeChartPanel(_props: { scope: FilterScope; tradeNo: number }) {
  return <div className="notice">Phase 4 — candlestick reconstruction with fills, MAE/MFE, VWAP.</div>;
}
